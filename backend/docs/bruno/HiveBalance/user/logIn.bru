meta {
  name: logIn
  type: http
  seq: 2
}

post {
  url: {{BACKEND_URL}}/login
  body: json
  auth: none
}

headers {
  X-CSRF-TOKEN: {{CSRF_TOKEN}}
}

body:json {
  {
      "loginIdentifier": "user1@demo.ch",
      "password": "astrongpassword"
  }
}
